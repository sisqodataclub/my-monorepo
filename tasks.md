🚨 CRITICAL SYSTEM DIRECTIVE: DO NOT copy, duplicate, or reuse any existing applications in the `apps/` directory (like `ddeep`). You MUST initialize a completely fresh, empty application from scratch using the terminal. 🚨

1. DIRECTORY: 
   - Navigate to `my-monorepo/apps/`

2. TECH STACK: React (Vite), Tailwind CSS, Recharts (or Tremor), and React Router v7.
3. DATA FETCHING: Use MOCK DATA ONLY. Create a `src/mockData.ts` file containing realistic JSON. Do NOT attempt to connect to the backend yet.
4. REQUIRED UI COMPONENTS (Business Analyst View):
   - TOP KPI CARDS: Total Revenue, Total Bookings, Average Order Value, Quote Conversion Rate.
   - CHARTS & GRAPHS: Revenue Trend (Line), Booking Funnel (Bar), Service Popularity (Donut), Customer Retention (Stacked Bar).
   - DATA TABLES: Recent "Contact Messages" and "Recent Bookings".
5. ROUTING: Set up basic routing for an "Overview" page and an "Inquiries" page.
